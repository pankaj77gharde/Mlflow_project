name: Mlflow_project

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      convSize: {type: tuple, default: (3,3)}
      epochs: {type: int, default: 1000}
    command: "python Train_TensorFlow.py {batch_size} {epochs}"
